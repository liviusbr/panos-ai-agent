# Copy this file to .env.sh, fill in real values, then:
#   chmod 600 .env.sh
#   source .env.sh
#
# .env.sh itself is gitignored — never commit real credentials into it.

export ANTHROPIC_API_KEY="sk-ant-..."
export PANOS_HOSTNAME="192.168.1.214"
export PANOS_USERNAME="admin"
export PANOS_PASSWORD="..."
